server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LaaLkA/analizator-config.git
          search-paths:
            - '{application}'
  application:
    name: config-server